# NEW-API encrypted backup transfer

This public repository is being used only as a temporary transport for an encrypted New API backup.

Do not commit plaintext backups, database files, token pools, or decryption keys here.
